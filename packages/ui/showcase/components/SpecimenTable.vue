<script setup lang="ts">
// SpecimenTable — the ONE hairline list that stages a token ladder: outer
// border-y, rows divided by inset hairlines (SpecimenRow). Six foundations
// pages used to each hand-write this pair; the shape now has a single home.
// (Not a library component — the host has no such surface; promote it only if
// a second consumer appears.)
</script>

<template>
  <div class="border-y border-border-soft">
    <slot />
  </div>
</template>
