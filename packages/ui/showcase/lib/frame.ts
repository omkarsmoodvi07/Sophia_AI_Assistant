// The showcase's ONE staged-surface tier: every frame that holds rendered
// instances — the doc spine's example stages and All-variants wall, the
// playground canvas frame, the color stack, the overview index cards —
// shares this trio. Padding varies by purpose and is appended at the call
// site; the trio itself is never re-typed.
export const STAGE_FRAME_CLASS = 'rounded-lg border border-border-soft'
