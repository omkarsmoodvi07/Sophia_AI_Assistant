# 仅当客户端真正发起协议升级（WebSocket）时才向上游传递 Connection: upgrade；
# 普通请求（含 SSE）保持标准的 Connection 语义。
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 8082;
    listen [::]:8082;
    server_name ~^.+\.browser\..+$;

    client_max_body_size 50m;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 60s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;

    location / {
        proxy_pass http://${SOPHIA_SERVER_UPSTREAM};
    }
}

# HTTP 入口。浏览器对同一 origin 的 HTTP/1.1 并发连接上限约 6 条，多标签页 /
# 多会话的 SSE 长连接可能占满配额导致普通请求排队（#822）。挂载 TLS 证书即可
# 额外启用 HTTPS + HTTP/2 入口（h2 多路复用彻底绕开该限制），
# 见 templates-available/https.conf.template 与 15-sophia-https.sh。
server {
    listen 8082 default_server;
    listen [::]:8082 default_server;
    server_name _;

    include /etc/nginx/conf.d/includes/sophia-app.conf;
}
